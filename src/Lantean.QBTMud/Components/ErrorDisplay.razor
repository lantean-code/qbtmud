<MudList T="string">
    <MudListItem OnClick="ClearErrors">@WebUiLocalizer.Translate("AppErrorDisplay", "Clear Errors")</MudListItem>
    <MudListItem OnClick="ClearErrorsAndResumeAsync">@WebUiLocalizer.Translate("AppErrorDisplay", "Clear Errors and Resume")</MudListItem>
    <MudDivider />
    @foreach (var error in Errors)
    {
        <MudListItem OnClick="@(e => ShowException(error))">@error.Message</MudListItem>
    }
</MudList>
