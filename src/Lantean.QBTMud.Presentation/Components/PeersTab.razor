<div class="peers-tab">
<MudMenu @ref="ContextMenu" Dense="true" PositionAtCursor="true" ListClass="unselectable" PopoverClass="unselectable">
    <MudMenuItem Icon="@Icons.Material.Filled.AddCircle" IconColor="Color.Info" OnClick="AddPeer">@TranslatePeerList("Add peers...")</MudMenuItem>
    @if (ContextMenuItem is not null)
    {
        <MudMenuItem Icon="@Icons.Material.Filled.ContentCopy" IconColor="Color.Info" OnClick="CopyPeerContextMenu">@TranslatePeerList("Copy IP:port")</MudMenuItem>
        <MudMenuItem Icon="@Icons.Material.Filled.DisabledByDefault" IconColor="Color.Info" OnClick="BanPeerContextMenu">@TranslatePeerList("Ban peer permanently")</MudMenuItem>
    }
</MudMenu>

<ContentPanel>
    <ToolbarContent>
        <MudToolBar Gutters="false" Dense="true">
            <MudIconButton Icon="@Icons.Material.Filled.AddCircle" Color="Color.Info" OnClick="AddPeer">@TranslatePeerList("Add peers...")</MudIconButton>
            <MudIconButton Icon="@Icons.Material.Filled.DisabledByDefault" Color="Color.Error" OnClick="BanPeerToolbar" Disabled="@(SelectedItem is null)">@TranslatePeerList("Ban peer permanently")</MudIconButton>
            <MudDivider Vertical="true" />
            <MudIconButton Icon="@Icons.Material.Outlined.ViewColumn" Color="Color.Inherit" OnClick="ColumnOptions" title="@TranslateApp("Choose Columns")" />
        </MudToolBar>
    </ToolbarContent>

    <ChildContent>
        <DynamicTable @ref="Table"
                      T="Peer"
                      TableId="Peers"
                      ColumnDefinitions="Columns"
                      ColumnFilter="FilterColumns"
                      ColumnFilterState="@(ShowFlags)"
                      Items="Peers"
                      MultiSelection="false"
                      SelectOnRowClick="true"
                      OnTableDataLongPress="TableDataLongPress"
                      OnTableDataContextMenu="TableDataContextMenu"
                      SelectedItemChanged="SelectedItemChanged"
                      Class="details-list content-panel__table" />
    </ChildContent>
</ContentPanel>
</div>
